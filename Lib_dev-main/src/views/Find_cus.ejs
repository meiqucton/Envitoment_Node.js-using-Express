<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/find.css" type="text/css" rel="stylesheet">
    <link href="/css/NavigationBar.css " type="text/css" rel="stylesheet">

</head>
<body>
    <div class="topnav">
        <a class="active" href="/Home">Home</a>
        <a href="/Home/Addbooks">Add Book</a>
        <a href="/search">Find Books</a>
        <a href="/Profile">Account</a>
        <a href="/Cus_Show">Customer List</a>
        <a href="/Book_show">Book List</a>
        <a href="/NewPaper">Find Name</a>
        
      </div>
    <form action="/searchId" method ="GET">
        <label for="CustomId">Author:</label>
        <input type="number" id="CustomId" name="CustomId" placeholder="Enter author CustomId">
        <button type="submit">Search</button>
    </form>
    <% if (results && results.length > 0) { %>
        <table>
            <thead>
                <tr>
           

                    <th>UserName</th>
                    <th>Gmail</th>
                    <th>Phone</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach(function(result) { %>
                    <tr>
                  
  
                        <td><%= result.UserName %></td>
                        <td><%= result.Gmail %></td>
                        <td><%= result.Phone %></td>
                        <td>
                            <button>Info</button></a>
                            
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% } else { %>
        <p>No results found.</p>
    <% } %>
</body>
</html>